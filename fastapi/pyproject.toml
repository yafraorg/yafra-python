[project]
name = "yafra-fastapi"
version = "0.1.0"
description = "FastAPI sample application"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.115.6",
    "fastapi-cli==0.0.7",
]

[tool.uv]
package = false