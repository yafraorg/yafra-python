[project]
name = "yafra-graphs"
version = "0.1.0"
description = "2D and 3D plotting samples"
requires-python = ">=3.12"
dependencies = [
    "matplotlib==3.10.0",
    "numpy==2.2.2",
]

[tool.uv]
package = false