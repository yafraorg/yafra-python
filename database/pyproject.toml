[project]
name = "yafra-database"
version = "0.1.0"
description = "Database samples for Yafra Python"
requires-python = ">=3.12"
dependencies = [
   "pymysql",
   "sqlalchemy",
]

[tool.uv]
package = false